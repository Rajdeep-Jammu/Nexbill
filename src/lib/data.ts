// This file is now deprecated as we are fetching data from Firestore.
// The content is kept to prevent breaking imports, but it should not be used.
export const getProducts = () => {
  return [];
};
